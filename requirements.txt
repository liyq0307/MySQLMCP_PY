fastmcp>=2.0.0
asyncmy>=0.2.8
pydantic>=2.0.0
python-dotenv>=1.0.0
openpyxl>=3.1.0
pandas>=2.0.0
psutil>=5.9.0
cryptography>=41.0.0
asyncio-mqtt>=0.13.0

# 标准库模块（通常不需要单独安装）
# hashlib, gzip, zipfile, subprocess, pathlib, tempfile, shutil, asyncio, uuid, json, os, re

# 额外的企业级功能依赖
aiofiles>=23.0.0          # 异步文件操作
redis>=4.5.0              # 缓存和队列
sqlalchemy>=2.0.0         # ORM支持
click>=8.0.0              # 命令行接口
rich>=13.0.0              # 终端美化输出